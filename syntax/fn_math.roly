fn my_gcd (a: int, b: int) {
    while (b != 0) {
        r = a - a / b * b
        a = b
        b = r
    }
    return a
}

fn my_lcm (a: int, b: int) {
    return a / my_gcd(a, b) * b
}

x = my_gcd(48, 18)
y = my_lcm(4, 6)
print(x)
print(y)
