fn my_gcd (a, b) {
    while (b != 0) {
        r = a - a / b * b
        a = b
        b = r
    }
    return a
}

fn banner (text) {
    print("=" + text + "=")
}

fn my_lcm (a, b) {
    return a / my_gcd(a, b) * b
}

banner("roly")

x = my_gcd(48, 18)
y = my_lcm(4, 6)
print(x)
print(y)
