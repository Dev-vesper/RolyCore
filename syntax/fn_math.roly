fn gcd (a: int, b: int) {
    while (b != 0) {
        r = a - a / b * b
        a = b
        b = r
    }
    return a
}

fn lcm (a: int, b: int) {
    return a / gcd(a, b) * b
}

x = gcd(48, 18)
y = lcm(4, 6)
print(x)
print(y)
