"""Token definitions for the Roly language.

Each token has a type and, for some types, a value (e.g. a number or an
identifier name). Token types live here so the lexer can produce them and
the parser can consume them without circular imports.
"""
